# Codex vs Claude Code Skills — moved

> **This doc has moved.** The Codex-vs-Claude-Code comparison is now an appendix of the
> provider-agnostic **[`skill-portability.md`](./skill-portability.md)** (hub-and-spoke:
> the shared `agentskills.io` core plus a per-provider spoke), which scales past two
> providers. See its **"Appendix: Codex vs Claude Code at a glance"** section for the
> original pairwise table.

This stub is kept so existing references resolve. New writing belongs in
[`skill-portability.md`](./skill-portability.md).
