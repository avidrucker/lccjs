# compiled C reference binaries (rebuild with: gcc -O2 -o <name> <name>.c)
donut_ref
donut_frame
donut_A
donut_B
donut_frame_16bit
# scratch / intermediate capture frames (frame_simple.txt is the committed golden)
frame_raw.txt
frame_ref23.txt
frame_16bit.txt
frame_B.txt
# compiled C helper binaries (rebuild: gcc -O2 -o <name> <name>.c)
gen_cordic_vectors
gen_mul_vectors
donut_probe
verify_mul_algo
# LCC build artifacts + author cache (regenerated by the run_*_test.sh scripts)
*.o
*.e
*.lst
*.bst
name.nnn
*_actual.txt
