# compiled C reference binaries (rebuild with: gcc -O2 -o <name> <name>.c)
donut_ref
donut_frame
donut_A
donut_B
donut_frame_16bit
# scratch / intermediate capture frames (frame_simple.txt is the committed golden)
frame_raw.txt
frame_ref23.txt
frame_16bit.txt
frame_B.txt
# vector generator binary (rebuild: gcc -O2 -o gen_cordic_vectors gen_cordic_vectors.c)
gen_cordic_vectors
# LCC build artifacts + author cache (regenerated by run_cordic_test.sh)
*.o
*.e
*.lst
*.bst
name.nnn
cordic_actual.txt
