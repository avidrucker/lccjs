; program that tests the r command
        r
        nl

        mov r0, 3
        mov r1, 3
        mov r2, 11

        r
        nl

        mov r0, -1
        mov r1, 1
        add r2, r0, r1

        r 

        halt